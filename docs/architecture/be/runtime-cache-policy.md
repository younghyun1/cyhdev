# Runtime cache policy

PostgreSQL, object storage, source files, environment configuration, and live connections remain authoritative. A cache may drop or reject an entry only when its caller can read through to that authority, the data is explicitly approximate, or admission failure is surfaced. Personalized HTTP responses are never cached globally. Bounds are constants near the owning adapter so a later configuration change cannot silently create an unbounded process-wide collection.

## Inventory

| State | Authority | Bound and admission | Expiry and invalidation | Metrics |
| --- | --- | --- | --- | --- |
| Account sessions | The single backend process is authoritative; restart invalidates every session | 16,384 entries; atomic reservation prevents concurrent overflow, expired entries are purged once before saturation returns HTTP 503, and keys are full SHA-256 digests of 256-bit bearer secrets | One-hour absolute expiry with lookup-time removal, current-device login rotation, explicit logout, user-wide revoke, account-mutation refresh, and hourly purge | Current length, explicit saturation response/logging, and expiry-job pruned/remaining fields |
| Authentication abuse windows | The single backend process is authoritative; restart clears all attempt windows | 16,384 source-IP window records and 32,768 normalized identity/token window records; novel keys fail closed with HTTP 429 at capacity, only process-keyed SHA-256 digests are retained, and IPv6 sources aggregate by `/64` | Endpoint-specific one-minute through one-day fixed windows; minute sweep removes expired records and an existing key replaces its own expired records | Rejection events expose endpoint, digest dimension, capacity state, and retry duration; cleanup logs removed and retained counts |
| Blog metadata, slug, and order maps | PostgreSQL | 10,000 recent posts and at most 10,000 slug/order entries; a newer post evicts the oldest `(created_at, post_id)` entry, while an older candidate or an orphan slug is rejected at capacity | Startup replacement plus write-through update/delete; listing always uses database count/page/tag batches, and search hydration reads all cache misses in two database queries | Hits, misses, evictions, rejected admissions, database read-throughs, retained rows, and slug rows in synchronization logs |
| Blog search index | PostgreSQL | Durable on-disk Tantivy index; writer memory is fixed at 50 MB, while a 512-row keyset cursor bounds each database rebuild page | Startup rebuild stages all published database rows before one commit; rollback preserves the prior readable index on a page failure, and post mutations update/delete individual documents | Indexed document count and rebuild/update logs |
| Visitor-board aggregate | PostgreSQL visitation rows | 10,000 coordinates; startup admits the highest counts with coordinate tie-breaking, then novel coordinates use first-admission until the next rebuild | Full rebuild at startup; existing coordinates update in place | Structured synchronization fields and logarithmic rejection logs |
| Visitor-log write-behind buffer | Buffered events until PostgreSQL accepts them | 4,096 distinct keys and 10,000 pending events; the event cap also keeps six bind values per row below PostgreSQL's 65,535-parameter limit, and new events are rejected when either cap is full | Drained every minute; failed writes re-admit only within the same caps | Flush count and cumulative rejected-admission fields |
| ISO countries, subdivisions, languages, and currencies | Fixed ISO reference tables | Exempt fixed reference data; maps are replaced rather than grown incrementally | Full startup/admin synchronization | Synchronized row-count logs |
| GeoIP maps | Versioned GeoIP files embedded with the deployment | Exempt fixed reference data loaded once | Deployment replacement | Startup load-duration log |
| UI internationalization snapshot | PostgreSQL and checked-in UI text bundles | 50,000 null-subdivision UI entries and 64 MiB estimated retained key/content bytes; newest rows are admitted first and further rows are rejected deterministically | Atomic startup/admin replacement; a missing key or incomplete snapshot performs one batched PostgreSQL query for the requested and fallback locales, then admits only if both bounds allow it | Entries, retained/max bytes, completeness, hits, misses, rejected admissions, and database read-through counters |
| System metric history | Host measurements | 3,600 samples in a `VecDeque`; oldest sample is removed before admission | One sample per second; overwritten FIFO | Length and current CPU/memory accessors |
| Fastfetch output | Local `fastfetch` process | One string capped at 256 KiB on a UTF-8 boundary | Refreshed after one minute on access | Original/cached byte fields when truncated |
| WASM bundles | PostgreSQL compressed bundle columns | 256 MiB approximate retained-memory total, including per-entry metadata; deterministic FIFO eviction; an entry larger than the budget is served without admission | Full synchronization clears stale rows; upload/update upserts; delete invalidates | Entry count, used bytes, evictions, and rejected entries in structured logs |
| Live-chat messages | PostgreSQL | 128 MiB total with FIFO eviction; startup query also caps input at 50,000 rows | Full startup reload and write-through append | Admin cache-stats response exposes byte budget, bytes used, message count, and age range |
| Live-chat typing state | Live sockets | 4,096 actors; further typing entries are rejected | Four-second entry expiry and minute sweep; disconnect clears the actor | Admin cache stats expose active typing count |
| Live-chat rate windows | Live message attempts | 16,384 user/IP keys; saturation fails closed and treats a new key as abnormal | One-second window; minute sweep removes stale keys | Abnormal-message ban logs; no public per-key data |
| Live-chat connections | Live WebSockets | 4,096 connections; further upgraded sockets close before registration | Disconnect removal | Connected count in hello/presence and admin cache stats |
| Live-chat ban indexes | PostgreSQL security records | 50,000 user keys and 50,000 exact-IP keys; startup admits the newest 50,000 active records, duplicate subjects retain the permanent or latest-expiring ban, and novel subjects are rejected at capacity | Expired dominant entries drop lazily; startup replaces both indexes and newly persisted bans write through. If startup was truncated or an admission was rejected, every cache miss reads PostgreSQL; read-through errors fail closed | Per-index entries/max, completeness, hits, misses, database read-throughs, and rejected admissions in synchronization logs |
| RTC rooms and peer maps | Live WebRTC sessions plus PostgreSQL call records | Current API uses one fixed room; each room admits 1 to 64 participants from validated configuration. Per-peer publication keys are the fixed media enum; subscriptions derive from the bounded room roster | Empty-room removal and minute sweep; peer teardown clears publications/subscriptions | Occupancy, peer-state, and call persistence logs |
| Photograph batch sessions and item maps | Live batch workers; completed photographs are durable in PostgreSQL/object storage | 32 sessions; upload admission fails at capacity; each session has at most 50 items | Terminal entries expire after 30 minutes; stuck entries after six hours | Capacity warning and prune count logs |
| Photograph view write-behind buffer | Approximate PostgreSQL view counter | 8,192 photograph IDs; existing deltas coalesce and novel IDs are dropped at capacity | Drained periodically; failed writes re-admit within the same cap; deleted rows are not requeued | Cumulative rejected-event field with logarithmic warnings; flush-job count |

Request-local `HashMap`, `HashSet`, and `Vec` values are bounded by validated request/page/batch limits and are not runtime caches. Bounded Tokio channels, the Diesel pool, and the live-chat broadcast ring are concurrency controls rather than retained key-value caches; their capacities remain explicit at their construction sites.

## Deployment invariant

Session authority is intentionally process-local. Running multiple backend processes would create independent login state and is unsupported; process restart is the global session-revocation mechanism. This trades restart persistence for an O(1), database-free authenticated request path.
