# Website localization

- Status: complete implementation; final combined checks and commit are tracked in [parallel followups](2026-09-21-parallel-followups.md).
- Checkout: main at 2865879; localization files only.
- Completed: all eight locales have 650 keys, including newly localized call controls. Corrected pre-existing drift between 635 browser keys and 398 backend keys, then added 15 call keys. Secondary languages load as separate chunks; server failure retains local translations. Native selector names never depend on the current language. Script-aware Chinese negotiation, locale persistence, stale response protection, and exact title preservation are covered by tests. Corrected authenticated desktop header overlap with wrapped navigation and measured content offsets.
- Verification: `npm --prefix solid-csr-spa-template run test -- src/i18n/locales.test.ts`: 10 passed. `npm --prefix solid-csr-spa-template run test:e2e:chromium -- localization.spec.ts`: 14 passed, covering native names under all eight selections, offline fallback/reload, stale response, mobile overflow, and authenticated German header widths 768/1024/1280/1440/1920. Separate desktop suite: 6 passed. Typecheck and zero-warning lint passed. `cargo test --locked --package rust-be-template --lib features::i18n::`: 4 passed before the subsequent cache regression test was added. PostgreSQL 18 integration verifies all 5,200 source rows, idempotent synchronization, distinct Chinese script rows, and cold/warm readback for every locale.
- Remaining: combined final gates and commit. Independent native-speaker review has not been performed. User-authored content, long About essays, and embedded third-party applications remain unchanged. Several legacy date/time formatters still follow browser preferences rather than the selected UI language.

Existing fixed country/language reference rows cover all requested locales; no schema migration is needed. Chinese scripts use distinct country keys (156 and 158) under language 30, with script-aware BCP 47 tags.

The PostgreSQL test found a pre-existing cold-cache defect: a new cache incorrectly claimed completeness, so an earlier English read could suppress the database lookup for Korean. New caches now start incomplete; a full cache synchronization is required to establish completeness. The integration regression covers cold English-to-other-locale reads as well as fully synchronized reads.

Translation coverage and source ownership are documented in [UI localization](../design/fe/localization.md). Fixture login renders for all eight locales and desktop header renders are under `target/localization-renders/`; generated images remain untracked. The Traditional Mandarin source was converted from the Simplified translation with authoring-time OpenCC, then reviewed and adjusted for interface terminology. No conversion dependency is shipped.
