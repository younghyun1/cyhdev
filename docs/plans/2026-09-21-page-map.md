# Complete page map

Status: complete. Baseline: `main` at `b5ac42d`, equal to `origin/main` before edits.

## Outcome

Added `docs/design/fe/site-map.md` with Mermaid page hierarchy and access-flow diagrams, all 33 normalized SPA route states, source ownership, and four separately served browser surfaces. Linked it from root and frontend instructions and the documentation index. Route/menu/guard changes now require a matching map update.

## Decisions

Treat page hierarchy separately from exact clickable transitions. Distinguish signed-in and superuser guards from backend write authority. Represent photograph detail as a URL-synced gallery modal. Include Swagger, squaremap, EU5, and data-driven WASM content so the map extends beyond the SPA router. Do not enumerate runtime-uploaded demo instances as fixed routes.

## Verification and next action

A read-only TypeScript AST comparison verified all 33 normalized route states against both the inventory and page diagram, including source owners and access classifications: 24 public, five signed-in, three superuser, and one fallback. All 47 relative links across the map and updated indexes/instructions resolve. `git diff --check` passed. Mermaid source is included; renderer compatibility has not been exercised. No application behavior changed; application builds and tests were unnecessary. No remaining page-map work; visitor-map stacking and a page-render audit are separate follow-up scope.
