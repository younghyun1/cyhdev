# Code license and copyright footer

- Status: complete; 2026-09-22. License, attribution, shared desktop metrics row, and repository link implemented.
- Checkout: `main`; layout refinement starts from `ca0a6aa`. Original license implementation: `3c254f5`.
- Decision: MIT permits reuse and derivatives while requiring preservation of copyright and permission notices; it does not mandate visible website credit. The frontend already declares MIT. Repository history starts in 2025, so the notice covers 2025-2026. Preserve third-party licenses and distinguish code from personal site content.
- Sources: [OSI MIT license](https://opensource.org/license/mit), consulted 2026-09-22.
- Implementation: root license and README scope; explicit “Code licensed under the MIT License.” wording with Code linking to the repository in a separate tab. At wide desktop sizes copyright is centered on the same grid row as the final timing metrics, rather than adding a fourth line. Narrow desktop columns and mobile wrapping avoid overlap. The mobile status opener is a native button separate from the link; keyboard activation of Code cannot open status details.
- Verification: `cargo xtask frontend-check` passed typecheck, lint, 118 unit tests, and bundling. `cargo xtask clippy` passed native and WASM checks with existing unused-dependency manifest warnings. `npm --prefix solid-csr-spa-template run test:e2e:chromium -- e2e/page-polish.spec.ts e2e/mobile.spec.ts --grep 'search alignment|form focus|shared system status|WCAG' --workers=4 --output=../target/license-footer-tests` passed six checks, including 320/390/1440px footer layout and mobile accessibility. Reviewed screenshots at 320px and 1440px. `cargo xtask secret-scan` and `git diff --check` passed. No open PR exists for `main`.
- Correction: the first typecheck caught the existing clock signal's nullable initial value; the copyright year now initializes directly from the UTC date instead of subscribing to that clock. The full frontend gate passed after correction.
- Wording refinement verification: repeated `cargo xtask frontend-check` (118 tests), `cargo xtask clippy`, `cargo xtask secret-scan`, and `git diff --check` successfully. Three Chromium `page-polish.spec.ts` checks selected with `--grep 'search alignment'` passed at 320/390/1440px; reviewed the updated 320px render.
- Layout refinement verification: frontend checks (118 tests), Clippy, and confidential-information scan passed. Chromium `page-polish.spec.ts` and `mobile.spec.ts`, selected with `--grep 'search alignment|repository link|form focus|shared system status|WCAG'`, passed 11 tests at widths from 320 to 1920px, including same-row alignment, no overlap, repository keyboard activation, and accessibility. Reviewed desktop renders; retained the smaller copyright type size after the first render showed avoidable metric wrapping at 1440px.
- Remaining: no implementation work. Not deployed; no release build or live-service changes. Third-party license compliance is not a complete dependency audit.
